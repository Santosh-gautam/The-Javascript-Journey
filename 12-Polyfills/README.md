# Module 12: Polyfills

> **Notice**: Scope outline only — full chapter content not yet written

This module outlines polyfill implementations, showing how standard JavaScript API methods are built under the hood by recreating them from scratch.

## 🗺️ Future Polyfill Roadmap

- `[ ]` **12-01: Array Methods** — Implementing `map`, `filter`, `reduce`, and `flat` array prototypes.
- `[ ]` **12-02: Function Prototypes** — Recreating `call`, `apply`, and `bind` bindings.
- `[ ]` **12-03: Promises** — Building the core `Promise` class and helper methods like `Promise.all` or `Promise.race`.
- `[ ]` **12-04: Utilities** — Creating custom versions of `Object.create`, `structuredClone`, and object builders.
